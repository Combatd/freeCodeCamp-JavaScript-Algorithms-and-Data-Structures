{"index.js":"function bouncer(arr) {\n  // Don't show a false ID to this bouncer.\n  let properID = arr.filter(Boolean);\n  return properID;\n  // the filter method filters out all false values from the array\n}\n\nbouncer([7, \"ate\", \"\", false, 9]);"}