{"index.js":"function rot13(str) { // LBH QVQ VG!\n let alphabet = \"ABCDEFGHIJKLMNOPQRSTUVWXYZ\";\n let regex = /[A-Z]/; // leave other characters alone\n var strArr = str.split('');\n console.log(strArr);\n    \n var newStr = \"\";\n    // map will return new array while calling function that iterates and checks each character\n    var result = str.split('').map.call(str, function(char) {\n          // Convert char to a character code\n          var letter = char.charCodeAt(0);\n          // keep it jumping out of array\n          if (letter < 65 || letter > 90) {\n            return String.fromCharCode(letter); // it is in the correct range\n          }\n          // move it forward into range\n          else if (letter < 78) {\n            return String.fromCharCode(letter + 13);\n          }\n          // move it back into range\n          return String.fromCharCode(letter - 13);\n        }).join('');  // Rejoin the array into a string\n    \n    console.log(result);\n    return result;\n}\n\n// Change the inputs below to test\nrot13(\"SERR PBQR PNZC\");"}