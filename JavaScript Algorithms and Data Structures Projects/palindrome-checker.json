{"index.js":"function palindrome(str) {\n  // Good luck\n  const regEx = /[^A-Za-z0-9]/g; // remove special characters that aren't letters, including underscore\n  \n  // lowercase, remove spaces\n  var newStr = str.toLowerCase().replace(regEx, '')\n\n  console.log(newStr);\n\n  let begin = 0; // iterate the normal way\n  let end = newStr.length - 1; // we will use this to iterate the other way\n\n  // meets in the middle?\n  for (var i = begin; i < end/2; i++) {\n    if (newStr[i] !== newStr[end-i]) {\n      return false; // each character much match or false\n    }\n }\n\n return true; // the check above did not match, so it must be true\n  \n}\n\n\n\npalindrome(\"eye\");"}